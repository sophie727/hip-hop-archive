// TODO: maybe move these from public/assets to a google folder so it's not so ridiculously full lol
const tree_art_map = (art) => {
  return `/assets/${art}.png`;
};

export const tree_types = ["sakura", "wisteria", "magnolia", "tabebuia"];

export default tree_art_map;
